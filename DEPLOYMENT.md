# Awesome-2025 배포 가이드

이 문서는 **Awesome-2025** 프로젝트 (Next.js + Sanity)를 Vercel에 배포하는 방법을 설명합니다.

## 1. 사전 준비 사항

배포를 진행하기 전에 다음 계정들이 필요합니다.

- **GitHub 계정**: 코드가 저장된 리포지토리
- **Vercel 계정**: Next.js 애플리케이션 호스팅
- **Sanity 계정**: CMS 데이터 관리

## 2. 환경 변수 설정

프로젝트의 `.env.example` 파일을 참고하여, 배포 시 필요한 환경 변수들을 미리 준비해야 합니다. Vercel 배포 과정에서 이 값들을 입력해야 합니다.

| 변수명 | 설명 | 예시 값 |
| :--- | :--- | :--- |
| `NEXT_PUBLIC_SANITY_PROJECT_ID` | Sanity 프로젝트 ID | `pv8y...` |
| `NEXT_PUBLIC_SANITY_DATASET` | 데이터셋 이름 | `production` |
| `NEXT_PUBLIC_SANITY_API_VERSION` | Sanity API 버전 | `2024-12-09` |

> 💡 **Tip**: Sanity 프로젝트 ID는 `sanity.json` 또는 [Sanity Manage](https://www.sanity.io/manage) 대시보드에서 확인할 수 있습니다.

## 3. Vercel 배포 방법

Vercel은 Next.js를 만든 팀에서 제공하는 호스팅 서비스로, 가장 쉽고 최적화된 배포 경험을 제공합니다.

1.  [Vercel 대시보드](https://vercel.com/dashboard)에 로그인합니다.
2.  오른쪽 상단의 **"Add New..."** 버튼을 클릭하고 **"Project"**를 선택합니다.
3.  **"Import Git Repository"**에서 `awesome-2025` 리포지토리 옆의 **"Import"** 버튼을 클릭합니다.
4.  **"Configure Project"** 화면에서 다음을 설정합니다:
    *   **Project Name**: 원하는 프로젝트 이름을 입력합니다.
    *   **Framework Preset**: `Next.js` (자동 감지됨)
    *   **Root Directory**: `./` (기본값)
    *   **Environment Variables**: 위에서 확인한 환경 변수들을 키(Key)와 값(Value)으로 추가합니다.
5.  **"Deploy"** 버튼을 클릭합니다.
6.  배포가 완료되면 축하 메시지와 함께 생성된 도메인(예: `https://awesome-2025.vercel.app`)을 확인할 수 있습니다.

## 4. Sanity CORS 설정 (필수)

배포된 웹사이트에서 Sanity 데이터에 접근할 수 있도록 도메인을 허용해주어야 합니다. 이 설정을 하지 않으면 데이터가 불러와지지 않습니다.

1.  [Sanity Manage](https://www.sanity.io/manage)로 이동하여 해당 프로젝트를 선택합니다.
2.  상단 메뉴에서 **"API"** 탭을 클릭합니다.
3.  **"CORS origins"** 섹션으로 스크롤하여 **"Add CORS origin"** 버튼을 클릭합니다.
4.  다음 정보를 입력합니다:
    *   **Origin**: 배포된 Vercel 도메인 (예: `https://awesome-2025.vercel.app`)
    *   **Allow credentials**: ✅ 체크 (사용자 인증이 필요한 경우 필요)
5.  **"Save"**를 클릭하여 저장합니다.

## 5. 배포 확인

배포된 Vercel URL로 접속하여 웹사이트가 정상적으로 로딩되는지 확인합니다.
- 메인 페이지의 콘텐츠가 Sanity에서 가져온 데이터로 잘 표시되는지 확인합니다.
- Sanity Studio (`/studio` 경로 등)가 포함되어 있다면 해당 경로로 접속하여 CMS가 작동하는지 확인합니다.
